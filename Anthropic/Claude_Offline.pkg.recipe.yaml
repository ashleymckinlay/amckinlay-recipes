Description: |
  Builds separate arm64 and x86_64 component packages, then wraps them in a
  top-level installer whose postinstall script installs only the package that
  matches the device architecture.
Identifier: com.github.ashleymckinlay.pkg.Claude_Offline
ParentRecipe: com.github.ashleymckinlay.download.Claude_Offline
MinimumVersion: '2.9'

Input:
  NAME: Claude_Offline
  SOFTWARE_TITLE: Claude

Process:
  - Processor: AppPkgCreator
